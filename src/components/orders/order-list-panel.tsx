"use client";
import Link from "next/link"; import { useState } from "react";
import { customerRequest } from "@/components/customers/customer-api";
import type { SalesOrderDto } from "@/server/orders/order-service";
type Customer={id:string;code:string;name:string};
export function OrderListPanel({initial,customers,canWrite}:{initial:SalesOrderDto[];customers:Customer[];canWrite:boolean}){const [items,setItems]=useState(initial),[customerId,setCustomerId]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState(""); async function create(){if(!customerId){setError("거래처를 선택해 주세요.");return}setBusy(true);setError("");try{const row=await customerRequest<SalesOrderDto>("/api/v1/orders",{method:"POST",body:JSON.stringify({customerId})});setItems(v=>[row,...v]);setCustomerId("")}catch(e){setError(e instanceof Error?e.message:"수주를 만들지 못했습니다.")}finally{setBusy(false)}}return <div className="space-y-5"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h1 className="text-xl font-black">수주 목록</h1><p className="mt-1 text-sm text-slate-500">절곡 항목과 금액은 다음 단계에서 이 수주에 추가됩니다.</p>{canWrite?<div className="mt-4 flex flex-col gap-2 sm:flex-row"><select aria-label="새 수주 거래처" value={customerId} onChange={e=>setCustomerId(e.target.value)} className="h-10 min-w-0 rounded border border-slate-300 bg-white px-3 text-sm"><option value="">거래처 선택</option>{customers.map(c=><option key={c.id} value={c.id}>{c.code} · {c.name}</option>)}</select><button type="button" disabled={busy} onClick={()=>void create()} className="h-10 rounded bg-teal-700 px-4 text-sm font-bold text-white disabled:opacity-50">{busy?"생성 중":"새 수주"}</button></div>:null}{error?<p role="alert" className="mt-3 text-sm text-red-700">{error}</p>:null}</section><section className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">{items.length?items.map(x=><Link className="block border-b border-slate-100 px-5 py-4 last:border-0 hover:bg-slate-50" href={`/orders/${x.id}`} key={x.id}><div className="flex items-center justify-between gap-3"><b>{x.orderNumber}</b><span className={x.status==="DRAFT"?"rounded bg-blue-100 px-2 py-0.5 text-xs font-bold text-blue-800":"rounded bg-slate-200 px-2 py-0.5 text-xs font-bold text-slate-700"}>{x.status==="DRAFT"?"작성 중":"취소"}</span></div><p className="mt-1 text-sm text-slate-700">{x.customer.code} · {x.customer.name}</p><p className="mt-1 text-xs text-slate-500">납기 {x.dueDate??"미지정"} · 수정 {new Date(x.updatedAt).toLocaleString("ko-KR")}</p></Link>):<p className="px-5 py-16 text-center text-sm text-slate-500">등록된 수주가 없습니다.</p>}</section></div>}
